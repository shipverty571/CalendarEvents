<Application x:Class="View.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:View"
             xmlns:viewModel="clr-namespace:ViewModel;assembly=ViewModel"
             xmlns:control="clr-namespace:View.Controls">
    <Application.Resources>
         <DataTemplate DataType="{x:Type TypeName=viewModel:DayInfoVM}">
             <control:DayInfoControl />
         </DataTemplate>
        <DataTemplate DataType="{x:Type TypeName=viewModel:CalendarVM}">
            <control:CalendarControl />
        </DataTemplate>
        <DataTemplate DataType="{x:Type TypeName=viewModel:EventsManagementVM}">
            <control:EventsManagementControl />
        </DataTemplate>
        <DataTemplate DataType="{x:Type TypeName=viewModel:MessageVM}">
            <control:MessageControl />
        </DataTemplate>
    </Application.Resources>
</Application>
